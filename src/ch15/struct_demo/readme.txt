# 结构体实例在内存中的布局

内存中的连续的存储空间：存放着结构体中定义的而不同变量，未初始化之前：为各个类型的默认值
初始化后，则变为了初始化后的值。

bool类型：false
数据型：0
string：""
指针类型：map slice 等，默认类型是未分配内存空间，nil，必须make分配空间后才可以进行试用

## struct 结构体的变量类型为值类型，而非引用类型

## 结构体的字段在内存中是连续分布的
